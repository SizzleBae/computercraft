import { EventHandler } from "./utils/EventHandler";

print("Hello World!")

const handler = new EventHandler();

class Test {
    private a: number = os.time();
}

const [name, key] = os.pullEvent()


print(name, key)
handler.listen();